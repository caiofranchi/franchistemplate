{% extends 'admin/master_with_menu.twig' %}

{% block main_content %}

        <div class="page-header">
            <h1 class="">/{{ page_name }} <small>/{{ action }}</small></h1>
            <a href="/admin/categorias/create" id="submit-button" data-loading-text="Loading..."  class="btn btn-primary">Add New</a>
        </div>

        <!-- SEARCH-->
        <form class="navbar-form navbar-right page-header" role="search">
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Search" required="required">
            </div>
            <button  type="submit" class="btn btn-default glyphicon glyphicon-search"></button>
        </form>


        <!--TABLE-->
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>#id</th>
                    <th>Nome</th>
                    <th>Ordem</th>
                    <th>Descrição</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                {% for row in table %}
                    <tr>
                        <td>{{ row.id }}</td>
                        <td>{{ row.nome }}</td>
                        <td>{{ row.ordem }}</td>
                        <td>{{ row.descricao }}</td>
                        <td>

                            <a href="{{ siteUrl }}categorias/{{ row.id }}" class="btn btn-default btn-xs" title="EDIT">
                                <span class="glyphicon glyphicon-pencil"></span>
                            </a>

                            <button type="button" class="btn btn-default btn-xs" title="REMOVE">
                                <span class="glyphicon glyphicon-remove"></span>
                            </button>
                        </td>
                    </tr>
                {% endfor %}

                </tbody>
            </table>
        </div>

        {% if totalPages>1 %}
        <ul class="pagination">
            <li {% if currentPage==1 %} class="disabled" {% endif %}><a href="/admin/categorias/page/{{ previousPage }}">&laquo;</a></li>
            {% for i in 1..totalPages %}
                <li {% if currentPage==i %}class="active"{% endif %}><a href="/admin/categorias/page/{{ i }}">{{ i }}</a></li>
            {% endfor %}
            <li {% if currentPage==totalPages %} class="disabled" {% endif %}><a href="/admin/categorias/page/{{ nextPage }}">»</a></li>
        </ul>
        {% endif %}
{% endblock %}